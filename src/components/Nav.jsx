import { Navbar } from "react-bootstrap";

const Nav = () => {
  return ( 
    <Navbar bg="success">
    <Navbar.Brand href="#home">Spotify playlist tool</Navbar.Brand>
  </Navbar>
   );
}
 
export default Nav;